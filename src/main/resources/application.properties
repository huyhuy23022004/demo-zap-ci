server.port=8080

# Expose health endpoint for CI readiness check
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

spring.thymeleaf.cache=false
spring.application.name=demo
